/* ===== src/firebase.js ===== */
'use strict';
// Placeholder — initialize Firebase app/auth here when ready.
// Keys will come from config/firebase.config.js later.
const FirebaseApp = {
  ready: false,
  init() {
    // no-op until Firebase is wired
    this.ready = false;
  },
};
